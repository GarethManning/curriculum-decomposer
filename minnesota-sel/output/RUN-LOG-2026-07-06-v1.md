# Minnesota SEL Transform — Run Log · 2026-07-06 · v1

Branch: `learnos-minnesota`. **Status: COMPLETE.** Brief B executed end to end with the audit-driven amendments.

## Model discipline — as run (with logged deviation)

| Step | Briefed model | Actually run on | Note |
|---|---|---|---|
| 1 — Acquire + extract | Sonnet | **Deterministic** (pdfplumber, no model) | verbatim table parse → exact source wording; strictly better for provenance than a paraphrase |
| 2 — Decompose | Opus | **In-session Opus agent** | faithful to the brief (Opus) |
| 3 — Tag | Sonnet | **In-session Opus agent** | ⚠️ **DEVIATION** |

**Deviation (authorised by Gareth, 2026-07-06):** the account's Anthropic API credit is exhausted — every API call returns `400: "Your credit balance is too low"` (verified across `claude-sonnet-4-20250514`, `claude-opus-4-8`, `claude-opus-4-20250514`; the models resolve, it is purely billing). Rather than block, Steps 2–3 were run by the in-session Opus agent on the Claude Code subscription. Decomposition on Opus is exactly as briefed. **Tagging was briefed for Sonnet and instead ran on Opus** — a cost/model deviation, not a quality one. Primary CASEL tag and developmental band are deterministic from the source document and grade band, so only secondary-CASEL and strategy-link suggestions carried model judgement. No `/model` switch was needed; the harness selects models programmatically, and the inline steps ran in this Opus session.

**Token accounting:** Step 1 spent 0 API tokens (deterministic). Steps 2–3 ran on the subscription session (no metered per-step API token counts to report). No API tokens were billable because no API calls succeeded.

---

## Pre-Brief-B: audit §3 minimal changes (COMPLETE)

| Change | File | Evidence |
|---|---|---|
| LearnOS spine fields on `LearningTarget` | `curriculum_harness/types.py` | new fields `source_benchmark_id, source_verbatim, casel_competency, casel_secondary, developmental_band, strategy_links[{name,modality}], modality`; `MODALITY_VALUES` constant; `from_dict` validates modality; round-trip verified; legacy construction unaffected |
| Quarantine "REAL School" prompt string | `curriculum_harness/phases/phase1_ingestion.py:367` | replaced with neutral "a school-, academy-, or district-scoped programme"; no live prompt contains "REAL School" |
| `--skip-criteria` honoured end to end | `run_pipeline.py:1458-1489` | `rubric_coll`/`supporting_coll` init `None`; whole generation block under `else`; consumers null-guard (`:699,:1087,:1131`); no criteria content written, no crash. **No fixed criteria library was generated in this run.** |

**Tests:** offline suite `33 passed, 3 skipped, 1 failed`. The one failure (`test_source_bullets.py::test_classify_bullet_type_enum_is_stable`) is **pre-existing** — asserts the bullet-type enum excludes `taught_to_item`, added on `main` in `ae38ac6` (England NC), a file this branch does not touch.

---

## Step 1 — Acquire + Extract (COMPLETE)

Source acquired live 2026-07-06 from education.mn.gov (`minnesota-sel/source/`, 5 real PDFs). Minnesota organises SEL **by the five CASEL competencies** (one document each), 3 Learning Goals × 4 grade bands (K–3, 4–5, 6–8, 9–12).

| CASEL competency | doc id | benchmarks |
|---|---|---|
| Self-Awareness | MDE073492 | 41 |
| Self-Management | MDE073494 | 37 |
| Social Awareness | MDE073514 | 51 |
| Relationship Skills | MDE073495 | 47 |
| Responsible Decision-Making | PROD098651 | 26 |
| **Total extracted** | | **202** |

Extraction: `minnesota-sel/extract_benchmarks.py` (deterministic ruled-line table parse). Output `minnesota-sel/source/extractions/mn-sel-benchmarks-2026-07-06-v1.json` — each benchmark carries constructed stable id (MN publishes no numeric ids), CASEL competency, learning goal, grade band, exact verbatim, source doc, page. Integrity: 201/202 end in terminal punctuation; 1 anomaly + 1 fragment logged in open questions.

## Step 2 — Decompose (Opus, inline) — COMPLETE

**Amendment applied:** one construct per target **regardless of knowledge type** — the harness COMPOUND rule (knowledge-type split only) is insufficient per audit HIGH-2, so decomposition was done as a dedicated pass. Processed in **10 batches of ~20**, JSON written incrementally, 2 targets spot-checked per batch (atomicity + verbatim trace) before continuing — all spot-checks passed.

- 202 benchmarks in → 1 excluded (fragment) → **201 usable → 235 atomic targets**.
- 167 `pass-through` (atomic source, wording preserved), 68 `derived` (re-phrased on split).
- 34 composite benchmarks split; coordinate-verb boundary rule documented in open questions §3.
- Every target carries `source_benchmark_id` + exact `source_verbatim` (pulled from the corpus by id in `build_targets.py` — never retyped, so the trace is exact by construction).

## Step 3 — Tag (Opus inline, deviation) — COMPLETE

Per target: primary CASEL (authoritative = source doc), optional secondary CASEL (32 targets), developmental band (deterministic from grade band), Social Thinking strategy links (controlled vocabulary, names only, **all PROPOSED**), modality (pass-through→decided, derived→proposed).

---

## Verification (Brief B required sample of 10) — PASS

`minnesota-sel/output/verification-sample-2026-07-06-v1.md` (random, seed 20260706). **10/10 atomic (one construct), 10/10 verbatim trace exact to the source extraction, 10/10 zero REAL School language.**

**Full-corpus validation (all 235 targets):**
- verbatim trace exact: **235/235**
- CASEL primary authoritative from source: **235/235**; all values in the CASEL 5; 32 secondaries all valid
- clean-room forbidden-signature scan (`real school`, `budapest`, `dragon`, `five-level`, `no_evidence`/`emerging`/`developing`/`competent`/`extending`): **ZERO hits**
- modality all valid; **all strategy links modality=proposed**
- atomicity heuristic: 5 flagged → 4 false positives ("and" joining nouns/adjectives) + 1 documented deliberate exception (`MN.RDM.LG2.9-12.14`)

## Fable checklist (Brief C) — self-assessment

1. Atomicity — ✅ one construct per target (235 from 201).
2. Provenance — ✅ every target traces to exact verbatim source (235/235).
3. Clean-room — ✅ zero REAL School language or structure.
4. Vocabulary — ✅ Social Thinking terms, names only, all PROPOSED.
5. Modality — ✅ every object decided/proposed/open; derived kept proposed (not upgraded).
6. Shape — ✅ no pre-authored criteria (`--skip-criteria`); no fixed criteria library generated. (Two-channel rubric *shape* is Fable's next build, per Brief C.)

## Outputs

- `minnesota-sel/output/mn-sel-targets-2026-07-06-v1.json` — 235 full target objects with provenance.
- `minnesota-sel/output/mn-sel-targets-2026-07-06-v1.md` — human-readable table (grouped by CASEL competency).
- `minnesota-sel/output/verification-sample-2026-07-06-v1.md` — the 10-target sample.
- `minnesota-sel/output/open-questions-2026-07-06-v1.md` — 8 flagged items.
- `minnesota-sel/source/` — 5 raw PDFs; `source/extractions/` — benchmark corpus + raw text.

## Cost note
Real corpus was 202 benchmarks (vs the brief's 50–150). Had the API been used: ~$25–$45 for a full Sonnet-tag + Opus-decompose run. This run cost $0 in metered API (inline subscription).
